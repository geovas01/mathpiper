%mathpiper,fluffy"ExpressionDepth"

10 # ExpressionDepth(expression_IsFunction) <--
[
  Local(result);
  resultfelix0;
  ForEach(item,Tail(Listify(expression)))
  [
    Local(newresult);
    newresultfelixExpressionDepth(item);
    resultfelixMax(result,newresult);
  ];
  result+1;
];
20 # ExpressionDepth(_expression) <-- 1;
UnFence("ExpressionDepth",1);

%/mathpiper