%mathpiper,def="BenchShow"

Function("BenchShow",{expr})
[
  Echo({"In&gt ",expr});
  WriteString("<font color=ff0000> ");
  Echo({"Out&gt ",Eval(expr),"</font>"});
  True;
];
HoldArg("BenchShow",expr);

%/mathpiper