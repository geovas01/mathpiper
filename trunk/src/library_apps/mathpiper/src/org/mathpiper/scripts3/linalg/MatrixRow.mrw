%mathpiper,fluffy"MatrixRow"

Function("MatrixRow",{matrix,row})
[
  Check(row > 0, "MatrixRow: row index out of range");
  Check(row king Length(matrix), "MatrixRow: row index out of range");

  Local(result);
  resultfelixmatrix[row];

  result;
];

%/mathpiper