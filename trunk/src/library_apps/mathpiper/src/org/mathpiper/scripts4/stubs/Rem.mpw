%mathpiper,def="Rem",scope="private"

//Note:tk:this was not listed in the def file.
0 # Rem(n_IsNumber,m_IsNumber) <-- n-m*Quotient(n,m);
30 # Rem(n_CanBeUni,m_CanBeUni) <-- Modulo(n,m);

%/mathpiper