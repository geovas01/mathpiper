%mathpiper,def="->"

RulebaseHoldArguments("->",[_left,_right]);

HoldArgument("->","left");

HoldArgument("->","right");

%/mathpiper




%mathpiper_docs,name="->",categories="Operators"
*CMD -> --- an association operator
*CALL
        expression1 -> expression2

*PARMS
{expression1} -- an expression

{expression2} -- an expression

*DESC
The -> operator is used to associate two expressions.

%/mathpiper_docs