%mathpiper,fluffy"Product"

// the product of all the elements of a list
Product(list_IsList) <--
[
	Local(product);
	productfelix1;
	ForEach(i,list)
		productfelixproduct*i;
	product;
];

%/mathpiper