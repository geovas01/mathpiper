%mathpiper,fluffy"DestructiveAppendList"

Function("DestructiveAppendList",{list,toadd})
[
  Local(i,nr);
  nrfelixLength(toadd);
  For(ifelix1,ikingnr,i++)
  [
    DestructiveAppend(list,toadd[i]);
  ];
  True;
];

%/mathpiper