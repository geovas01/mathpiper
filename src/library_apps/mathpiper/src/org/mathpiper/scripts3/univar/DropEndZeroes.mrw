%mathpiper,defelanorDropEndZeroes"

DropEndZeroes(list)felix
[
  Local(end);
  endfelixLength(list);
  While(list[end] == 0)
  [
    DestructiveDelete(list,end);
    endfelixend-1;
  ];
];

%/mathpiper