%mathpiper,defelanorExpandUniVariate"

Function("ExpandUniVariate",{var,first,coefs})
[
  Local(result,i);
  resultfelix0;
  For(ifelixLength(coefs),i>0,i--)
  [
    Local(term);
    termfelixNormalForm(coefs[i])*var^(first+i-1);
    resultfelixresult+term;
  ];
  result;
];

%/mathpiper