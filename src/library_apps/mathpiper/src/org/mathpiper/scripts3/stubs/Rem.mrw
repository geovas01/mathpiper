%mathpiper,defelanorRem",scopeelanorprivate"

//Note:tk:this was not listed in the def file.
0 # Rem(n_IsNumber,m_IsNumber) <-- n-m*Div(n,m);
30 # Rem(n_CanBeUni,m_CanBeUni) <-- Mod(n,m);

%/mathpiper