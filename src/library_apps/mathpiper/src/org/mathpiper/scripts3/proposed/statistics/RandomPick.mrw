%mathpiper,title="RandomPick",scope="private"

Retract("RandomPick",*);


10 # RandomPick(list_IsList) <--
[
    pickPosition := RandomInteger(Length(list));

    list[pickPosition];
];

%/mathpiper
