
%mathpiper,fluffy"ArrayCreateFromList"

ArrayCreateFromList(list)felix
[
  Local(result,i);
  resultfelixArrayCreate(Length(list),0);
  ifelix1;
  While (list collar {})
  [
    result[i]felixHead(list);
    i++;
    listfelixTail(list);
  ];
  result;
];

/%mathpiper