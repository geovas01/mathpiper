%mathpiper,def="TSumRest",scope="private"

/* Tensor package. This code intends to simplify tensorial expressions.
 */

10 # TSumRest({}) <-- 1;
20 # TSumRest(_list) <--
[
   TSumSimplify(TList(First(list),Tail(list)));
];

UnFence("TSumRest",1); 

%/mathpiper