%mathpiper,def="WriteLine"

WriteLine(writer, line) :=
{	
	line := JavaCall(writer, "write", line);
};

%/mathpiper





%mathpiper_docs,name="WriteLine",categories="Programming Functions;Input/Output",access="experimental"

*CMD WriteLine --- writes a line to the
*CALL
        WriteLine(writer, line)

*PARMS
{writer} -- 

{line} -- line to add to specified file

*DESC
This function writes a line to a program.

*E.G.
In> WriteLine("Some text.");

%/mathpiper_docs
