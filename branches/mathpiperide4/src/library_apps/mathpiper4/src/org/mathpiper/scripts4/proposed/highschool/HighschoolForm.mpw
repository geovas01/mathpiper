%mathpiper,def=""

HighschoolForm(expression) :=
[
    //Note: since := is at a higher precedence than :/, parentheses are needed.
    expression := (expression /: [ (x_NegativeNumber?) / y_ <- [Echo(x,/,y);]]);
    
    expression := (expression  /: [z_^((x_NegativeInteger?)/y_Number?) <- [z,x,y]]);
    
];


%/mathpiper

    %output,preserve="false"
      Result: True
.   %/output


